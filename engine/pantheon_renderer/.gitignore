# Built artefacts and staged third-party binaries. Sources and recipe only:
# build_host.sh rebuilds everything here from the vendored tree.
build/
*.exe
*.o

# Mesa 24.3.3 i686 plus its dependency closure -- 2.3 GB of staged binaries.
# Provenance and SHA-256 for every package are in gl_software/PROVENANCE.md,
# which IS tracked.
gl_software/bin/

# WolfcamQL 11.3 source, extracted from the archive whose SHA-256 is pinned in
# SOURCE.sha256. Vendored for building, not redistributed from here.
wolfcamql-11.3-src/
