"""PANTHEON prologue: the three-part opening, and the facts that pay for it.

    facts       every number the prologue may show, re-derived on demand
    primitives  the visual vocabulary, as SVG driven by real data
    slots       named holes only human review is allowed to fill
    proof       builds the proof sheet from all three

Read-only with respect to the archive. This package never writes to a cache
and never modifies code owned by the review workstation.
"""
